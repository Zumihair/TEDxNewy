The newsletter row that closes the homepage: one email field and a red pill.

```jsx
<SubscribeForm cta="Subscribe" />
```

Sits on the deep-red (`--red-deep`) closing section under a short warm invitation, never as a modal or a floating bar.
