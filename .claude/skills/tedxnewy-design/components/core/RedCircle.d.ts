export interface RedCircleProps {
  /** Diameter in px. */
  size?: number;
  opacity?: number;
  style?: React.CSSProperties;
}

export declare function RedCircle(props: RedCircleProps): JSX.Element;
