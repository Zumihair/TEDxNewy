A lit red sphere echoing the red circle on the TEDx stage. Decoration only, never a control.

```jsx
<RedCircle size={280} opacity={0.9} style={{ position: "absolute", right: -60, top: 40 }} />
```

Use at most one per view, bleeding off an edge of a dark section.
