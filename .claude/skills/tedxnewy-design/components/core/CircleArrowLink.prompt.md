The site's headline CTA on dark sections: a plain label followed by a red circle holding a white arrow, the circle sliding right on hover.

```jsx
<CircleArrowLink href="/events/reframe-2025" size="md">See how it went</CircleArrowLink>
```

Use it once per section, under a big heading. On cream surfaces pass `color="var(--ink)"`. It replaces, not accompanies, a pill button.
