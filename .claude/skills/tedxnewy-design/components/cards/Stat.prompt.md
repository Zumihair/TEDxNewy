One number from the season, sized like a headline. The site runs four across a solid red band.

```jsx
<Stat value="100" suffix="%" label="Volunteer-run" sub="Not-for-profit" />
```

Keep the numbers real and modest — the brand's stats band is deliberately honest ("5 events", "since 2024"), not inflated. Never more than four in a row.
