# Build / runtime
node_modules/
.next/
out/
*.log
*.tsbuildinfo
next-env.d.ts

# Local env / Vercel
.env
.env.local
.env.*.local
.vercel/

# OS
.DS_Store
Thumbs.db

# Sibling projects in the same parent directory — not part of TEDxNewy
clarifiai/
claused-mobile/
clever-ui/
commex/
conditionproof/
impeccable/
npxskillui/
payproof/
piku-template/
reckono/
taste-skill/

# Stray files
claused-report-editor-mockup.html

# Local Claude Code / IDE configs (skills are the exception: the two
# design-system skills under .claude/skills/ are versioned so every
# session on any machine gets them after a pull)
.claude/*
!.claude/skills/

# Source-only working asset folders at project root (the deployed copies live in public/)
/Brand/
/Images/

# Raw speaker image drop folder — only the README is tracked
/raw-speaker-images/*
!/raw-speaker-images/README.md

# Raw event photo drop folder — only the README is tracked
/raw-event-photos/*
!/raw-event-photos/README.md
.vercel
.env*.local
